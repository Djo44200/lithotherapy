{% extends 'base.html.twig' %}

{% block title %}Reload{% endblock %}

{% block body %}
    <h1>Reload</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reload.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reload.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reload_index') }}">back to list</a>

    <a href="{{ path('reload_edit', {'id': reload.id}) }}">edit</a>

    {{ include('reload/_delete_form.html.twig') }}
{% endblock %}
