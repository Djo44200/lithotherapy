{% extends 'base.html.twig' %}

{% block title %}Editer une dureté{% endblock %}

{% block body %}
    <h1 class="text-muted">Editer une dureté </h1>


    {{ form_start(form) }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-6 ">
                {{ form_label(form.number) }}
                {{ form_widget(form.number) }}
                {{ form_errors(form.number) }}

            </div>
        </div>
        <div class="row">
            <div class="col-2">
                <br>
                {{ form_label(form.submit) }}
                {{ form_widget(form.submit) }}
                {{ form_end(form) }}
            </div>
            <div class="col-2">
                <br>
                <a href="{{ path('color_index') }}">
                    <button class="btn bouton">Retour</button>
                </a>
            </div>
            <div class="col-2">
                <br>

            </div>
        </div>
    </div>
    </div>

{% endblock %}
