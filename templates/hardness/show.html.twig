{% extends 'base.html.twig' %}

{% block title %}Hardness{% endblock %}

{% block body %}
    <h1>Hardness</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hardness.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ hardness.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hardness_index') }}">back to list</a>

    <a href="{{ path('hardness_edit', {'id': hardness.id}) }}">edit</a>

    {{ include('hardness/_delete_form.html.twig') }}
{% endblock %}
