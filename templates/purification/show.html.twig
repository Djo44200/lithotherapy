{% extends 'base.html.twig' %}

{% block title %}Purification{% endblock %}

{% block body %}
    <h1>Purification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purification.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ purification.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('purification_index') }}">back to list</a>

    <a href="{{ path('purification_edit', {'id': purification.id}) }}">edit</a>

    {{ include('purification/_delete_form.html.twig') }}
{% endblock %}
